<Page
    x:Class="FolderMapMappingProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FolderMapMappingProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" x:Name="Open_folder_button" Content="Select folder" HorizontalAlignment="Center" Margin="10,10,10,10" VerticalAlignment="Center" Click="openButton_Click"/>
        <TextBlock Grid.Row="0" x:Name="mainFolderNameDisplay" HorizontalAlignment="Left" Margin="697,16,0,0" TextWrapping="Wrap" Text="" VerticalAlignment="Top"/>
        <StackPanel Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Orientation="Horizontal" Margin="10,10,10,10">
            <ListView x:Name="MainFolder"/>
        </StackPanel>

    </Grid>
</Page>
